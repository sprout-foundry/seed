name: CI

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        go: ['1.21', '1.22', '1.23']
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go }}
          cache: true

      - name: Vet
        run: go vet ./...

      - name: Format
        run: |
          if [ -n "$(gofmt -l .)" ]; then
            echo "Code is not formatted. Run 'go fmt ./...'"
            gofmt -l .
            exit 1
          fi

      - name: Build
        run: go build ./...

      - name: Test
        run: go test -race -count=1 ./...

  example:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: stable
          cache: true
      - name: Run example
        run: go run ./example/minimal/

  release:
    needs: test
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: stable
          cache: true

      - name: Build binaries
        run: |
          mkdir -p dist
          GOOS=linux GOARCH=amd64 go build -o dist/seed-linux-amd64 ./example/minimal/
          GOOS=linux GOARCH=arm64 go build -o dist/seed-linux-arm64 ./example/minimal/
          GOOS=darwin GOARCH=amd64 go build -o dist/seed-darwin-amd64 ./example/minimal/
          GOOS=darwin GOARCH=arm64 go build -o dist/seed-darwin-arm64 ./example/minimal/

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          files: 'dist/*'
          generate_release_notes: true
