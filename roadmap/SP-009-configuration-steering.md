# SP-009: Configuration, Steering & Extensibility

**Status:** ✅ Complete — see [docs/extensibility.md](../docs/extensibility.md)

RetryConfig customizable via Options. Steer/SteerSystem queue transient messages for next API call. SetProvider swaps provider at runtime. OnIteration callback fires at loop start. Compaction event published with strategy and token delta. Feature gates: DisableFallbackParser, DisableValidator, DisableNormalizer.
